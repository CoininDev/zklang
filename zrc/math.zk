// this is not a module, because it just has pure functions



// operators are simply functions
int operatorUwU(int left, int right) { left + right }

void using() +IO {
  let x = 6 UwU 7;
  let y = operatorUwU(6, 7);
  let z = 6 : operatorUwU(7);
  let w = operator+(11, 5);
  IOputs(x);
  IOputs(x == y && y == z);
  IOputs(operator&&(operator==(x, y), operator==(y z)));
}
